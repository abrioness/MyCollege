@model WebColegio.Models.ViewModel.RecuperarPasswordViewModel

@{
    Layout = null;
}
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Recuperar contraseña - Colegio San Francisco Javier</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/login.css" asp-append-version="true" />
</head>
<body>
    <div class="login-page">
        <section class="login-form-panel" style="max-width: none; flex: 1;">
            <div class="login-wrapper">
                <div class="login_container">
                    <div class="right">
                        <div class="center text-white text-center">
                            <div class="login">Recuperar contraseña</div>
                            <img src="~/Image/colegiosanfrancisco.png" class="logo" alt="Emblema del colegio" />
                        </div>
                        <form class="form" method="post" asp-action="RecuperarPassword">
                            <label asp-for="Identificador"></label>
                            <input asp-for="Identificador" placeholder="Usuario, cédula o correo" autocomplete="username" />
                            <span asp-validation-for="Identificador" class="text-danger"></span>
                            <input type="submit" id="submit" value="Enviar enlace" />
                            <div class="login-back">
                                <a asp-controller="Login" asp-action="Login">Volver al inicio de sesión</a>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </section>
    </div>

    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    @if (TempData["Mensaje"] != null)
    {
        <script>
            document.addEventListener("DOMContentLoaded", function () {
                var mensaje = @Html.Raw(Json.Serialize(TempData["Mensaje"]));
                Swal.fire({
                    title: '@(TempData["Tipo"]?.ToString() == "success" ? "Éxito" :
             TempData["Tipo"]?.ToString() == "warning" ? "Atención" :
             "Información")',
                    text: mensaje,
                    icon: '@TempData["Tipo"]',
                    confirmButtonText: 'Aceptar'
                });
            });
        </script>
    }
</body>
</html>
