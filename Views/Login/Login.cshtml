@model WebColegio.Models.ViewModel.UsuarioViewModel


@*
    VISTA MÍNIMA FUNCIONAL
    Elimina TODO y pega solo esto
*@

@{

    Layout = null;
}
<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8"  name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/login.css" asp-append-version="true" />
</head>
<body>
    <div class="login-wrapper">
<div class="login_container">
    

    
    <div class="right">
        <div class="center text-white">
            <div class="login">Iniciar Sesión</div>
            <img src="~/Image/colegiosanfrancisco.png" class="logo"/>
            @* <div class="eula">"En todo Amar y Servir""</div> *@
        </div>
        <form class="form" method="post" asp-action="Login" enctype="multipart/form-data">
            <label for="NombreUsuario">Usuario</label>
                    <i class="bi bi-person"></i><input type="text" name="NombreUsuario" id="user" required placeholder="Nombre de Usuario" />
            <label for="Password">Contraseña</label>
                    <i class="bi bi-person-lock"></i><input type="password" name="Password" id="password" required placeholder="Contraseña">
                    <i class="bi bi-person-check"></i><input type="submit" class="bi bi-person-fill-check" id="submit" value="Iniciar Sesión">
                

            @* ¿No tienes una Cuenta?<a href="@Url.Action("Create","Usuarios")"> Registrar!</a> *@
        </form>
        @* @if (TempData.ContainsKey("Mensaje"))
        {
            <div class="form-group">
                <div class="alert alert-danger" role="alert">
                    @TempData["Mensaje"]
                </div>

            </div>
        } *@
    </div>
    </div>

    

</div>


@* 
@section Scripts {

    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://code.jquery.com/ui/1.13.2/jquery-ui.min.js"></script>
    <link rel="stylesheet" href="https://code.jquery.com/ui/1.13.2/themes/base/jquery-ui.css" />
    <script src="~/js/limpiarcampos.js"></script>
    <script src="~/seewtalert/sweetalert.all.min.js"></script>
    <script src="~/seewtalert/sweetalertstyle.js"></script>
    <link href="~/seewtalert/sweetalert.min.css" rel="stylesheet" / *@>

    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

    @* <partial name="_ValidationScriptsPartial" />  *@
    @if (TempData["Mensaje"] != null)
    {
        <script>
            document.addEventListener("DOMContentLoaded", function () {
                var mensaje = @Html.Raw(Json.Serialize(TempData["Mensaje"]));
                Swal.fire({
                    title: '@(TempData["Tipo"]?.ToString() == "success" ? "Éxito" :
         TempData["Tipo"]?.ToString() == "warning" ? "Atención" :
         "Información")',
                    text: mensaje,
                    icon: '@TempData["Tipo"]',
                    confirmButtonText: 'Aceptar'
                });
            });
        </script>
    }
</body>
</html>
@* } *@
 
